'use client'
import { useReducer } from "react"

function reducer(state, action) {

}

export default function DiagnosticMain() {
  const init = {};
  const [state, dispatch] = useReducer(reducer, init);
  return (
    <main>
      <div>
      
      </div>
      <div>
        
      </div>
    </main>
  )
}